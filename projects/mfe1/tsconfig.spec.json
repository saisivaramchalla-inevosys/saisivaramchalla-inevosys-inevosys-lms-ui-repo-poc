/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  // Extends the base TypeScript configuration
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Specifies the output directory for compiled test files
    "outDir": "../../out-tsc/spec",
    // Includes type definitions for Jasmine testing framework
    "types": [
      "jasmine"
    ]
  },
  "include": [
    // Includes all spec files in the src directory and its subdirectories
    "src/**/*.spec.ts",
    // Includes all TypeScript declaration files in the src directory and its subdirectories
    "src/**/*.d.ts"
  ]
}
